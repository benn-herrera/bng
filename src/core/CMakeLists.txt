include("${CMAKE_INCLUDE}/target_lib.cmake")

#find_package(glm CONFIG REQUIRED)
#find_path(GLM_INCLUDE_DIR NAMES glm.hpp PATH_SUFFIXES glm)
#target_include_directories(${TARGET} PRIVATE ${GLM_INCLUDE_DIR})
#target_link_libraries(${TARGET} PRIVATE glm::glm-header-only)
